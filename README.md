# nids-drift-mlops
Automated Network Intrusion Detection System with concept drift detection and self-retraining — an end-to-end MLOps pipeline (MongoDB, MLflow, DVC, Evidently AI, FastAPI, Docker) that detects when a trained model goes stale on evolving traffic and automatically retrains itself, including live traffic monitoring.
